# P0 label-drift raw data pointer (2026-07-02)

Full per-cell row TSVs and every encoded AVIF from the two drift legs live in
block storage (not git; per large-file discipline):

- `/mnt/v/output/zenavif/drift-2026-07-02/drift_base.tsv` — 336 rows, leg
  `base` = registry zenrav1e 0.1.4 (path-identical route control; reproduced
  the canonical dataset's `encoded_bytes` **336/336 byte-identical** and
  `score_ssim2` exactly)
- `/mnt/v/output/zenavif/drift-2026-07-02/drift_master.tsv` — 336 rows, leg
  `master` = zenrav1e `64a081d4` (git-archive export, ravif dev-patch applied
  box-side only)
- `/mnt/v/output/zenavif/drift-2026-07-02/encoded_base/` + `encoded_master/`
  — all 672 encoded AVIFs (content-addressable by
  `<variant>__<cell>_q<q>.avif`), ~19 MB total
- Tower mirror: `/mnt/tower/output/zenavif/drift-2026-07-02/`

Produced by `examples/drift_reencode.rs` (sample:
`scripts/rd_gap/drift_sample.tsv` from `scripts/rd_gap/sample_drift_cells.py`;
box driver: `scripts/rd_gap/remote/run_drift.sh`) on Hetzner `zenavif-sweep-1`
(ccx63), snapshot after: `zenavif-sweep-1-1783025248`.

Summary percentiles: `benchmarks/drift_check_2026-07-02.tsv`.
Interpretation + P1 verdict: `docs/FEATURE_HINTS_PLAN.md` (P0 section).
