# Animation track Exif/XMP — 2026-09-07

A posterless sequence containing track-local Exif/XMP returned neither from
probe_animation_info. The executed reproduction in sidecars-before.log failed
on missing Exif. read_trak skipped meta, and the shared meta reader required a
primary image even for a track-local metadata container.

Track parsing now carries Exif/XMP extents and its own idat through color-track
association. Metadata parsing retains the caller's strict/lenient options and
keeps top-level primary-image validation separate. animation_exif() strips the
checked offset prefix and padding; animation_xmp() returns RDF/XML MIME items.
Arbitrary MIME items no longer masquerade as XMP. Primary-item accessors retain
independent payloads. Shared idat resolution checks bounds without unchecked
range arithmetic and retains multi-extent ordering.

Native eager/lazy animation info carries owned track Exif/XMP. Managed/AOM frame
conversion, animation probes and codec metadata select track sidecars, including
authoritative absence, and propagate malformed-payload errors. Deprecated eager
AnimationConfig and AvifParser::to_avif_data retain both fields too.

The source-backed integration fixture uses two real SVT samples at 17/33 ms.
Six layouts cover conflicting poster bytes, no poster, absent track metadata,
non-XMP MIME data, file-backed payloads and a padded file-backed Exif header.
Borrowed/owned parsers assert exact sidecar bytes. Native probes, eager/lazy
animation and managed/AOM codec adapters assert exact metadata; native and codec
frames match decoded control rows. Malformed Exif offsets fail explicitly while
the independent poster Exif remains intact. Extent units cover reordered ranges,
to-end ranges, missing idat/mdat and invalid bounds.

The 1501-byte parser regression fixture is generated by the parent integration
test's scenario 1. SHA-256:
fbccef0313eb3529f09c37f550bd5ba40905005d5ee6cac500dbcffa5a55a1b2.
The optional ZENAVIF_SIDECAR_ARTIFACTS variable exports all six files for reference
checks; assertions always execute regardless of this variable.

Libavif 1.3.0 independently decodes both frames in the posterless and padded
file-backed fixtures, reporting Exif 14 bytes and XMP 37 bytes. Logs are
sidecars-libavif-posterless.log and sidecars-libavif-file.log under
~/tmp/animation-metadata. These info checks prove presence/size and decoding;
exact bytes are additionally verified through libavif's PNG export. Its eXIf
chunk equals all 14 source TIFF bytes; its XMP iTXt chunk equals all 37 source
XML bytes. A Python binary-chunk check verifies every PNG CRC and both exact
payloads (sidecars-independent-bytes.log); no imaging-library dependency or
pixel-editing step is involved.

An additional executed eager regression caught a pre-existing early return:
without top-level meta, read_avif returned an empty AvifData, discarding frames
and track metadata. sidecars-eager-before.log fails with "posterless animation
discarded". The corrected branch extracts frames and carries track properties;
sidecars-eager-after.log passes 1/1, checking both direct eager parsing and
AvifParser::to_avif_data, exact sample bytes and both durations.

The first all-feature workspace run passes 887/887 (nine existing skips),
before the final extent/eager regression additions. Expanded sidecar integration
passes 3/3 and extent units 2/2. Determinism passes 25 legs and conformance passes
56 AVIF cells (optional armed CLI coverage explicitly unrun). Ladder retains
exactly the same 33 byte/quality failure rows plus 16 timing misses; monotone
retains the same two speed-6 screen/q80 inversions against speeds 7 and 8.
Those gates precede the final deprecated-eager-only correction; no encoder
production code was changed. Final all-feature workspace nextest passes 890/890 (nine existing skips);
default workspace tests/doctests pass 484 (ten existing ignores). All-feature
workspace library clippy passes with warnings denied; scoped formatting and
diff checks pass. Logs: sidecars-final-nextest.log, sidecars-final-default.log,
sidecars-final-clippy.log. Push and CI remain deferred.

Remaining objective includes additional metadata forms/associations, fractional
crop resampling, track matrices, non-square pixel presentation, auxiliary image
features, animation options/timing/mono APIs and video encoding. Existing
quality-envelope failures remain unresolved; no thresholds are repinned.
